# Demo

Run `/totalreclaw demo` to exercise the bundled read-only demo store.

The demo covers:

- plugin install / skill visibility issues
- missing tool dispatch / command routing issues
- conflicting gateway restart records

The demo does not modify the live memory store on the target host.
