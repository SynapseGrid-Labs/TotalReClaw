# Check

Run `/totalreclaw check "<task>"` or `/totalreclaw recall "<query>"` before a risky fix, install, config change, or debugging pass.

Expected outcomes:

- `prior_fix_found`
- `context_found`
- `no_match`
- `conflicting_memory`

Use the recommended next step as guidance, not as an auto-executed action.
